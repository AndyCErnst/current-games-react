'use strict';
var React = require('react'),
    NavLinks = require('./NavLinks');

var About = React.createClass({
  render: function() {
    return (
      <div>
        <h2>About Page</h2>
      </div>
      );
  }
})
        // <NavLinks />

module.exports = About;
